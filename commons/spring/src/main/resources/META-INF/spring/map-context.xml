<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
http://www.springframework.org/schema/beans
http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- The mappings themselves. Add resources to them with CustomMappings beans. -->
    <bean id="mappings"
          class="org.openehealth.ipf.commons.spring.map.SpringMappings"/>

    <!--
        The untyped MappingService over the very same mappings, for code that still needs one.
        Deprecated as of IPF 6.0; inject "mappings" in new code.
    -->
    <bean id="mappingService"
          class="org.openehealth.ipf.commons.spring.map.SpringBidiMappingService">
        <constructor-arg ref="mappings"/>
    </bean>

    <bean id="mappingExtension"
          class="org.openehealth.ipf.commons.map.extend.MappingExtension">
        <property name="mappingService" ref="mappings" />
    </bean>

    <bean id="customMappingsConfigurer"
          class="org.openehealth.ipf.commons.spring.map.config.CustomMappingsConfigurer">
        <property name="mappingService" ref="mappings" />
    </bean>

</beans>
