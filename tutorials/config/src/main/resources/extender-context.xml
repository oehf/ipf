<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:ipf="http://openehealth.org/schema/ipf-commons-core"
       xsi:schemaLocation="
http://www.springframework.org/schema/beans 
http://www.springframework.org/schema/beans/spring-beans.xsd
http://openehealth.org/schema/ipf-commons-core
http://openehealth.org/schema/ipf-commons-core.xsd">

  <!--
       Extension mechanism. Each of these beans collects its own contributions from the
       application context, so no post processor is needed:

       - the SpringMappings declared in base-context.xml picks up the CustomMappings beans
       - the registrar below picks up the DynamicExtension beans
       - the configurer below picks up the CustomRouteBuilder beans
  -->

  <!-- populates the ContextFacade, which the stateful Groovy extensions look beans up in.
       This used to be a side effect of the SpringConfigurationPostProcessor. -->
  <ipf:globalContext id="globalContext"/>

  <bean id="customExtensionModuleRegistrar"
        class="org.openehealth.ipf.commons.spring.core.extend.SpringDynamicExtensionRegistrar" />

  <bean id="customRouteBuilderConfigurer"
        class="org.openehealth.ipf.platform.camel.core.config.CustomRouteBuilderConfigurer">
    <property name="camelContext" ref="camelContext" />
  </bean>

</beans>