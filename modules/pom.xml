<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation=" http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.openehealth.ipf.modules</groupId>
    <artifactId>modules</artifactId>
    <name>modules</name>
    <packaging>pom</packaging>

    <parent>
        <groupId>org.openehealth.ipf</groupId>
        <artifactId>ipf</artifactId>
        <version>2.2-SNAPSHOT</version>
    </parent>

    <modules>
        <module>hl7dsl</module>
        <module>hl7</module>
        <module>cda</module>
    </modules>
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.gmaven</groupId>
                <artifactId>gmaven-plugin</artifactId>
                <executions>
                    <!-- Explicitly set the phases for stub generation and compilation --> 
                    <!-- This is needed for the Eclipse project generation with eclipse:eclipse -->
                    <!-- Maven 2.2.1 was not able to override or dropp an execution phase --> 
                    <!-- Define only the subset of goals used by all modules. Additional goals should be defined where needed --> 
                    <execution>
                    	<id>compile</id>
                        <phase>compile</phase>
       					<goals>
         					<goal>compile</goal>
       					</goals>
    				</execution>
                    <execution>
                    	<id>testCompile</id>
                        <phase>test-compile</phase>
       					<goals>
         					<goal>testCompile</goal>
       					</goals>
    				</execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
